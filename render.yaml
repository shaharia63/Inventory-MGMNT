services:
  - type: web
    name: inventory-management-system
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "22.16.0"
      - key: PORT
        value: 10000
    env: node
    plan: free